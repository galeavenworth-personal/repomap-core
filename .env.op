## Secret references for op run (1Password CLI)
## These are NOT secrets — they are pointers resolved at runtime by `op run`.
## Usage: op run --env-file .env.op -- <your-command>

KILO_API_KEY=op://Private/Kilo Gateway/credential
