# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
ENV/

# Build artifacts
build/
dist/
*.egg-info/

# Test / typecheck / linter caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Beads local DB cache (remote truth is on the beads-sync branch)
.beads/beads.db
.beads/*.db
.beads/*.db-*

# Beads sync-branch JSONL (kept in git history on `beads-sync`, not on main)
.beads/issues.jsonl
.beads/interactions.jsonl

# OS / editor metadata
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Local env files (secrets)
.env
.env.local

# Local workflow reasoning artifacts (keep out of git by default)
.kilocode/thinking/

# NOTE: `.kilocode/`, `.kilocodemodes`, and `.kilocodeignore` are intentionally tracked in this repo.
# Do NOT ignore them globally, or new workflow/tooling files won’t be added by default.

# plans
plans/

# Browser/tooling artifacts
screenshots/
